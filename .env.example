# Example environment variables for backend
HOST=127.0.0.1
PORT=8080
# STELLAR_SECRET=SB...  # Stellar secret seed for sending XLM (use testnet only)
# SOROBAN_RPC=https://soroban-testnet.stellar.org
